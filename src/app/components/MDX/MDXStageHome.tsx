import { ComponentPropsWithoutRef } from 'react'

import { StageHome } from '@/components/StageHome'

export const MDXStageHome = (props: ComponentPropsWithoutRef<'div'>) => (
  <StageHome {...props} />
)
