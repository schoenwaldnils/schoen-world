import { FC } from 'react'

import { ContainerTest as ContainerTestComponent } from './ContainerTest'

export default {
  title: 'ContainerTest',
  component: ContainerTestComponent,
}

export const ContainerTest: FC = () => (
  <ContainerTestComponent />
)
